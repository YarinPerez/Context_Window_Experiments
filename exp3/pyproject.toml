[project]
name = "rag-experiment"
version = "0.1.0"
description = "RAG vs Full Context Comparison Experiment"
requires-python = ">=3.10"
dependencies = [
    "chromadb>=0.4.0",
    "sentence-transformers>=2.0.0",
    "numpy>=1.24.0",
    "matplotlib>=3.7.0",
]
